<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recipeapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecipeApp">
        <activity
            android:name="presentation.RecipeAddActivity"
            android:exported="false"
            android:label="Add Recipe" />
        <activity
            android:name="presentation.RecipeListActivity"
            android:exported="false"
            android:label="Recipe List" />
        <activity
            android:name="presentation.RecipeViewerActivity"
            android:exported="false" />
        <activity
            android:name="presentation.GroceryListActivity"
            android:exported="false"
            android:label="Grocery List" />
        <activity
            android:name="presentation.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>